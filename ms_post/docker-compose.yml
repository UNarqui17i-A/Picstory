version: '2'
services:
  db:
    image: postgres
  app:
    build: .
    command:
      bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 8010 -b '0.0.0.0'"
    ports:
      - "8010:8010"
    depends_on:
      - db
